# ITK kagebot

## Intro

This robot has the important responsibility to remind people
who's turn it is to bring cake for the week.

## Arguments

The bot needs a list of initials as an argument. It will infer the email address from there.

It expects a json string in this format:

```json
{
    "name": "initials",
    "name": "initials",
}
```
